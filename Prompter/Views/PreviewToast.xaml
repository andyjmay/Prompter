<Window x:Class="Prompter.Views.PreviewToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Prompter.Views"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="False"
        ShowInTaskbar="False"
         SizeToContent="WidthAndHeight"
         MaxHeight="300"
         MinWidth="240"
        Loaded="Window_Loaded">
    <Border x:Name="RootBorder" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TitleText" Text="Prompter Output" FontWeight="Bold" Grid.Row="0"/>
            <ScrollViewer Grid.Row="1" Margin="0,8,0,8" VerticalScrollBarVisibility="Auto" local:SmoothScrollBehavior.IsEnabled="True">
                <TextBlock x:Name="OutputText" TextWrapping="Wrap"/>
            </ScrollViewer>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CopyButton" Content="Copy" Width="60" Margin="0,0,8,0" Click="Copy_Click" />
                <Button x:Name="DismissButton" Content="Dismiss" Width="60" Click="Dismiss_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>

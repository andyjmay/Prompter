<Window x:Class="Prompter.Views.PreviewToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="400"
        Height="160"
        Loaded="Window_Loaded">
    <Border Background="#FF2D2D2D" CornerRadius="8" Padding="16" BorderBrush="#FF444444" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Prompter Output" Foreground="White" FontWeight="Bold" FontSize="14" Grid.Row="0"/>
            <ScrollViewer Grid.Row="1" Margin="0,8,0,8" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="OutputText" Foreground="#FFE0E0E0" TextWrapping="Wrap" FontSize="12"/>
            </ScrollViewer>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Copy" Width="60" Margin="0,0,8,0" Click="Copy_Click" Background="#FF3C3C3C" Foreground="White" BorderBrush="#FF555555"/>
                <Button Content="Dismiss" Width="60" Click="Dismiss_Click" Background="#FF3C3C3C" Foreground="White" BorderBrush="#FF555555"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

<Window x:Class="Prompter.Views.TrayIconView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:vm="clr-namespace:Prompter.ViewModels"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="0" Height="0"
        Loaded="Window_Loaded"
        Closed="Window_Closed">
    <Grid>
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="/Assets/prompter.ico"
                        ToolTipText="Prompter"
                        LeftClickCommand="{Binding OpenSettingsCommand}"
                         MenuActivation="RightClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Mode" ItemsSource="{Binding ModeMenuItems}">
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding Name}"/>
                                <Setter Property="IsChecked" Value="{Binding IsSelected, Mode=OneWay}"/>
                                <Setter Property="IsCheckable" Value="True"/>
                                <Setter Property="StaysOpenOnClick" Value="False"/>
                                <Setter Property="Command" Value="{Binding DataContext.SetModeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <Setter Property="CommandParameter" Value="{Binding Id}"/>
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                    <MenuItem Header="Remove Filler Words (Clean)" IsCheckable="True" IsChecked="{Binding CleanEnabled}"/>
                    <MenuItem Header="Format Lists (List)" IsCheckable="True" IsChecked="{Binding ListFormattingEnabled}"/>
                    <Separator/>
                    <MenuItem Header="Settings" Command="{Binding OpenSettingsCommand}"/>
                    <MenuItem Header="Copy Last Output" Command="{Binding CopyLastOutputCommand}"/>
                    <Separator/>
                    <MenuItem Header="Exit" Command="{Binding ExitCommand}"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>

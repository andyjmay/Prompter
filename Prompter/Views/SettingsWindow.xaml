<Window x:Class="Prompter.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompter Settings"
        Width="420"
        Height="440"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Global Hotkey" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="HotkeyTextBox" Width="200" IsReadOnly="True" VerticalContentAlignment="Center" Margin="0,0,8,0"/>
            <Button x:Name="CaptureHotkeyButton" Content="Press to Record" Width="120" Click="CaptureHotkeyButton_Click"/>
        </StackPanel>

        <TextBlock Text="Default Mode" FontWeight="Bold" Grid.Row="2" Margin="0,0,0,4"/>
        <ComboBox x:Name="ModeComboBox" Grid.Row="3" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12">
            <ComboBoxItem Content="Standard"/>
            <ComboBoxItem Content="Formal"/>
            <ComboBoxItem Content="Raw"/>
        </ComboBox>

        <TextBlock Text="Model Idle Time (minutes)" FontWeight="Bold" Grid.Row="4" Margin="0,0,0,4"/>
        <Slider x:Name="TtlSlider" Grid.Row="5" Minimum="1" Maximum="30" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,4"/>
        <TextBlock x:Name="TtlValue" Grid.Row="6" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="7" VerticalAlignment="Bottom">
            <CheckBox x:Name="AutoStartCheckBox" Content="Start Prompter with Windows" Margin="0,0,0,8"/>
            <CheckBox x:Name="AudioFeedbackCheckBox" Content="Audio feedback on start/stop" Margin="0,0,0,12"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save" Width="80" Margin="0,0,8,0" Click="Save_Click"/>
                <Button Content="Cancel" Width="80" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>

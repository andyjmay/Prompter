<Window x:Class="Prompter.Views.RecordingOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowActivated="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="120">
    <Window.Resources>
        <Storyboard x:Key="PulseStoryboard" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="PulseDot"
                             Storyboard.TargetProperty="Opacity"
                             From="1.0" To="0.3"
                             Duration="0:0:0.8"
                             AutoReverse="True"/>
        </Storyboard>
    </Window.Resources>
    <Border x:Name="RootBorder" BorderThickness="1" Margin="0,16,0,0">
        <StackPanel x:Name="MainStack" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel x:Name="DotTextRow" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse x:Name="PulseDot" Width="14" Height="14" Margin="0,0,12,0"/>
                <TextBlock x:Name="StatusText" Text="Listening..." FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
            <ProgressBar x:Name="AudioMeter" Height="6" Margin="0,8,0,0" Maximum="1" Minimum="0" Value="0" Background="#22FFFFFF" BorderThickness="0" Visibility="Collapsed"/>
            <ProgressBar x:Name="ProcessingBar" Height="6" Margin="0,8,0,0" Background="#22FFFFFF" BorderThickness="0" Visibility="Collapsed" IsIndeterminate="True"/>
        </StackPanel>
    </Border>
</Window>
